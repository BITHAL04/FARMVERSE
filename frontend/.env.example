# Base URL for API (leave blank to use same origin + Vite proxy)
VITE_API_BASE_URL=
# Optional explicit proxy target override
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
# Backend will read OPENAI_API_KEY from root .env
# Example root .env entry (do NOT commit real key):
# OPENAI_API_KEY=sk-xxxx
